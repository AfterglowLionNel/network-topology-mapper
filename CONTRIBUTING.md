# コントリビューションガイド

バグ報告、文書改善、テスト、Pull Request を歓迎します。

## Issueを作る前に

- 脆弱性や個人情報の露出は公開 Issue に書かず、[SECURITY.md](SECURITY.md) の手順を使ってください。
- `output/`、実在する IP / MAC / SSID / ホスト名、ユーザー名、資格情報を添付しないでください。公開用レポートも内容を目視確認せず添付しないでください。
- 再現データが必要な場合は、文書用 IP アドレスや予約済みサンプル値へ置き換えてください。

## Pull Request

1. リポジトリをフォークし、変更ごとのブランチを作ります。
2. 変更理由と利用者への影響が分かる小さなコミットにします。
3. テストを追加または更新します。
4. 次を実行し、成功を確認します。

```powershell
pwsh -NoProfile -File .\tests\Invoke-Tests.ps1
```

ネットワーク探索を伴う手動確認は、自分が管理する LAN だけで行ってください。CI やテストで、実在する外部 IP、未知の LAN、ルーター管理画面へ接続しないでください。

UI 文言は、非 IT 利用者が理解できる平易な日本語にします。外部通信、通信量、管理者権限、設定変更が増える変更は、実行前の表示と関連文書も同時に更新してください。

Pull Request を提出すると、その貢献を本リポジトリの [MIT License](LICENSE) の下で提供することに同意したものとして扱います。第三者コードを含める場合は、出典、版、ライセンス、必要な通知を明記してください。
