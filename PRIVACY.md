# プライバシー

Network Topology Mapper はローカル優先で動作します。プロジェクト運営者の解析サーバー、ユーザー登録、テレメトリ、診断結果の自動アップロードはありません。利用者が Issue、Pull Request、AIサービス、クラウドストレージ等へ自分で添付した情報は、この方針の対象外です。

## ローカルに保存する情報

実行した機能と Windows から取得できる範囲に応じて、`output/` に次の情報が保存されることがあります。

- ローカル IP / IPv6 アドレス、MAC アドレス、ホスト名、端末名、メーカー推定
- PC 名、ユーザー名、ドメイン名、ネットワークアダプター、SSID、ゲートウェイ、DNS
- シリアル番号など Windows が返す機器情報
- ARP/近隣情報、開いている代表ポート、現在の接続、関連プロセス
- 公開 IP、ISP / ASN、NAT / STUN、回線品質、速度測定値
- 診断、監視、端末差分の履歴
- 利用者が `config/known-devices.json` に記入した名称とメモ

`output/` と `config/known-devices.json` は `.gitignore` の対象です。クラウド同期、バックアップ、画面共有、AIサービスへの貼り付けは別の送信経路になるため、利用者自身で管理してください。

通常の最新結果は同名ファイルへ上書きされます。端末差分スナップショットはネットワークごとに最大 30 件、診断値スナップショットは最大 120 件を保持します。不要になったデータは、アプリを閉じてから `output/` を利用者自身で削除できます。

## 外部通信

基本診断の既定値では外部サービスへ通信しません。`-ExternalChecks`、`-SpeedTest`、メニューでの明示的な選択などにより外部機能を有効にすると、各提供者は接続元の公開 IP、時刻、User-Agent など通常の通信メタデータを受け取れます。

速度測定では Cloudflare からデータを受信し、生成した測定用バイト列を Cloudflare へ送信します。診断結果ファイルそのものは送信しません。通信先と送信内容の一覧は [NETWORK_SERVICES.md](NETWORK_SERVICES.md) にあります。

## 公開用レポート

`-PublicReport` は、通常の調査結果から別の公開用ファイルを生成します。

- IPv4 はベンチマーク用アドレス帯、IPv6 は文書用アドレス帯へ置換
- MAC アドレスはローカル管理アドレスへ置換
- PC、ユーザー、ドメイン、SSID、端末名などを一貫した仮名へ置換
- 接続履歴、プロセス、外部回線情報、自由度の高い詳細を除外
- AI修正依頼も、仮名化後のレポート情報だけから生成

これは共有時のリスクを下げる「仮名化」であり、匿名化の保証ではありません。端末数、構成、時刻、測定値、珍しい機種名などの組み合わせから環境を推測できる場合があります。`diagram-public.html`、`devices-public.csv`、`ai-repair-prompt-public.txt` を公開する前に、必ず内容を目視確認してください。

## 資格情報

現在の公開版はルーター管理画面の資格情報を取得・保存しません。将来、資格情報を扱う機能を追加する場合は、保存先、暗号化、送信先、削除方法を実装と同時に文書化する必要があります。

## 問い合わせ

プライバシー上の問題を含む脆弱性は、公開 Issue に詳細を書かず [SECURITY.md](SECURITY.md) の手順で連絡してください。
